version: '3'

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB
  web:
    build: .
    command: python index.py
    volumes:
      - .:/code
    ports:
      - 5000:5000
    depends_on:
      - db
    env_file:
      - .env
